# Reference engines: studied, never built or committed here.
/ref/

/build/
/out/
.cache/
compile_commands.json
.DS_Store
*.exr
!tests/data/**/*.exr

# MODELS AND CAPTURES DO NOT LIVE HERE. Assets are fetched into
# ~/tools/assets and renders go to a scratch directory; the only things of
# this kind the repository holds are the four fixtures under tests/data and
# the codeless schema, which are kilobytes and are allowed back in by name.
*.usd
*.usdc
*.usdz
*.usda
!modules/usd/schemas/*.usda
*.fbx
*.blend
*.blend[0-9]
*.glb
*.gltf
*.abc
*.vdb
*.lrtc
*.ply
*.spz
*.sog
!tests/data/**/*.ply
!tests/data/**/*.sog
*.mp4
*.mov
*.zip
